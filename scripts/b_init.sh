#!/usr/bin/env bash

# Setup minikube (local only)
# minikube start --kubernetes-version=v1.20.0 --driver="hyperkit" --memory=8192 --cpus=4 --vm=true
# minikube addons enable ingress
# minikube addons enable istio-provisioner
# minikube addons enable istio
